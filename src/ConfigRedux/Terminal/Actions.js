export const setCandleInterval = (interval) => ({
    type: 'SET_CANDLE_INTERVAL',
    payload: interval
});

export const setGraphType = (interval) => ({
    type: 'SET_GRAPH_TYPE',
    payload: interval
});

export const setStrategy = (strategy) => ({
    type: 'SET_STRATEGY',
    payload: strategy
});
